import { Meta } from "@storybook/react";
import { Label } from ".";

export default {
  title: "atoms/Label",
  component: Label,
} as Meta;

const Template = () => <Label />;
export const Default = Template.bind({});
